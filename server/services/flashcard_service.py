"""Generate flashcards from document content using AI.

TODO: Implement flashcard generation with DeepSeek.
"""


def generate_flashcards(study_set_id: str, context_chunks: list[dict]) -> list[dict]:
    """Call AI to produce Q&A pairs from the provided context chunks."""

    # TODO:
    # Build a prompt asking the LLM to generate Q&A pairs from context
    # Parse the structured response
    # Save to DB
    # Return list of {question, answer, explanation}

    return []
